version: '3.8'
services:
  client:
    build:
      context: ./client
      dockerfile: ${CLIENT_DOCKERFILE:-Dockerfile}
      args:
        APP_ENV: ${APP_ENV:-production}
    image: web.dearmetoday.frontyard:${IMAGE_VERSION:-local}
    ports:
      - "${CLIENT_PORT:-8080}:80"
    restart: always
    depends_on:
      - server
    networks:
      - app-network

  server:
    container_name: server
    build:
      context: ./server
      dockerfile: Dockerfile
      args:
        VERSION: ${IMAGE_VERSION:-local}
    image: web.dearmetoday.backyard:${IMAGE_VERSION:-local}
    ports:
      - "3000:3000"
    environment:
      # ใช้ Supabase แทน DB local
      DATABASE_URL: ${DATABASE_URL}
      DIRECT_URL: ${DIRECT_URL}

      # พวก config อื่น ๆ
      PORT: 3000
      NODE_ENV: ${NODE_ENV:-production}
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_KEY: ${SUPABASE_KEY}
      SUPABASE_BUCKET: ${SUPABASE_BUCKET}
      SUPABASE_PUBLICURL: ${SUPABASE_PUBLICURL}
      RESEND_API_KEY: ${RESEND_API_KEY}
      SENDGRID_API_KEY: ${SENDGRID_API_KEY}
      MAIL_FROM: ${MAIL_FROM}
      FRONTEND_URL: ${FRONTEND_URL}
      SERVER_URL: ${SERVER_URL}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      JWT_SECRET: ${JWT_SECRET}
      COOKIE_DOMAIN: ${COOKIE_DOMAIN}
      COOKIE_SECURE: ${COOKIE_SECURE}
      COOKIE_SAMESITE: ${COOKIE_SAMESITE}
    networks:
      - app-network
    volumes:
      - ./server/uploads:/app/uploads
    restart: unless-stopped

networks:
  app-network:
    driver: bridge